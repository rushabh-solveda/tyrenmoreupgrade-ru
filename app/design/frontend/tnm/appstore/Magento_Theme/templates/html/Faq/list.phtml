<?php

/**
 * @category   Tyres
 * @package    Tyres_Faq
 * @author     anuj95455@gmail.com
 * @copyright  This file was generated by using Module Creator(http://code.vky.co.in/magento-2-module-creator/) provided by VKY <viky.031290@gmail.com>
 * @license    http://opensource.org/licenses/osl-3.0.php  Open Software License (OSL 3.0)
 */
?>
<?php

 $objectManager = \Magento\Framework\App\ObjectManager::getInstance();
            $cateId = $objectManager->get('Magento\Framework\Registry')->registry('current_category');

if (is_null($cateId)) {
	$cateId = 375 ;
}
else {
$cateId = $cateId->getId();

}

//$collection = $block->getCategorylist($cateId);

 //var_dump($cateId);
$collection = $block->getFaqCollection($cateId);
//var_dump($cateId);

if (    $collection->getSize() < 1      ){
$cateId = 375 ;
$collection = $block->getFaqCollection($cateId);
}





?>
         <?php foreach ($collection as $key => $data): ?>
                <tr>
                 <div class="accordion-item">
                     <div class="accordion-item-header">
                    <?php echo $data->getTitle(); ?>
                  </div>

                  <div class="accordion-item-body">
                        <div class="accordion-item-body-content">
                                 <?php echo $data->getContent(); ?>
                               </div>
                             </div>
                   </div>
                 </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

<?php// else: ?>

<?php //endif; ?>

    <script type="application/ld+json">

{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
	       <?php
$leng=$collection->getSize();
foreach ($collection as $key => $data): ?>
	{
      "@type": "Question",
      "name": "<?php echo $data->getTitle(); ?>",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "<?php $text=$data->getContent(); $cleantext=strip_tags($text); echo $cleantext ;  ?>"
      }
}<?php if( $leng > 1) echo ',';
        $leng--;
?>


     <?php endforeach; ?>
  ]
}

    </script>
